@model IEnumerable<SocietyAgendor.UI.Models.EstabelecimentoModel>

@{
    ViewData["Title"] = "Estabelecimento";
    Layout = "_LayoutLogado";
}



<div class="panel panel-default">
    <!-- Default panel contents -->
    <div class="panel-heading text-center">
        <h2> Estabelecimento</h2>
        <p>
            <a asp-controller="Estabelecimento" asp-action="CreateEstabelecimento" class="btn btn-default">Adicionar Estabelecimento</a>
        </p>
    </div>


    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Estabelecimento_Nome)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Estabelecimento_Email)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Estabelecimento_Telefone)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Estabelecimento_CNPJ)
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Estabelecimento_Nome)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Estabelecimento_Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Estabelecimento_Telefone)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Estabelecimento_CNPJ)
                </td>
                <td>
                    @Html.ActionLink("Editar", "UpdateEstabelecimento", new { estabelecimentoId = item.Estabelecimento_Id }) |
                    @Html.ActionLink("Excluir", "DeleteEstabelecimento", new { estabelecimentoId = item.Estabelecimento_Id })
                </td>
            </tr>
            }
        </tbody>
    </table>
</div>